<%- include('partials/header') %>

<div class="grade-container">
    <h2><%= grade %>학년 커리큘럼</h2>
    <p>아래 목록에서 학습하고 싶은 주제를 선택하세요:</p>
    
    <div class="curriculum-list">
        <% for (const curriculum of curriculums) { %>
            <a href="/grade/<%= grade %>/curriculum/<%= curriculum.index %>" class="curriculum-card">
                <div class="curriculum-title"><%= curriculum.title %></div>
                <div class="curriculum-arrow">→</div>
            </a>
        <% } %>
    </div>
    
    <div class="back-link">
        <a href="/" class="btn-back">← 홈으로 돌아가기</a>
    </div>
</div>

<style>
    .grade-container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
    }
    
    .grade-container h2 {
        color: #4CAF50;
        margin-bottom: 10px;
    }
    
    .curriculum-list {
        display: flex;
        flex-direction: column;
        gap: 15px;
        margin: 25px 0;
    }
    
    .curriculum-card {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 15px 20px;
        background-color: #f9f9f9;
        border-left: 5px solid #4CAF50;
        text-decoration: none;
        color: #333;
        border-radius: 5px;
        transition: transform 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
    }
    
    .curriculum-card:hover {
        background-color: #f0f0f0;
        transform: translateX(5px);
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    }
    
    .curriculum-title {
        font-size: 18px;
        font-weight: 500;
    }
    
    .curriculum-arrow {
        font-size: 20px;
        color: #4CAF50;
    }
    
    .back-link {
        margin-top: 30px;
    }
    
    .btn-back {
        display: inline-block;
        padding: 10px 15px;
        background-color: #f0f0f0;
        color: #333;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.2s ease;
    }
    
    .btn-back:hover {
        background-color: #e0e0e0;
    }
</style>

<%- include('partials/footer') %>
